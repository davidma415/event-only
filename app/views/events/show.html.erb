<h1><%= @event.name %></h1>
<%= link_to 'Back to Events', events_path %> | <%= link_to 'Edit', edit_event_path %> | <%= link_to 'Delete', event_path(@event), method: :delete, data: {confirm: 'Are you sure you want to delete this event?'} %>
<%= render 'event', event: @event %>
